export type IAddress = {
  id: string;
  lta: number;
  lng: number;
  department: string;
  province: string;
  district: string;
  urbanization: string;
  street: string;
  postalCode: number;
  fullAddress: string;
}